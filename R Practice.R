install.packages("ggplot2") # to install the packages

library(ggplot2) # to load the installed packages

old.packages() # to view the packages

update.packages() # to update the packages

detach("package:ggplot2",unload=TRUE) # to unload the packages

remove.packages("ggplot2") # to remove the packages

version # to know the R version

sessionInfo() # to know the session information

help(package = "ggplot2") # to see the selected package uses

install.packages("KernSmooth") # to install the packages